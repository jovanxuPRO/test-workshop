# PRD-TW-001：测试工坊 Pro 产品需求文档

## 版本历史
| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|----------|
| v1.0 | 2026-08-13 | 产品部 | 初稿（基于现有实现回溯） |
| v1.1 | 2026-08-13 | 产品部 | 增加按钮功能清单、认证测试章节、缺陷回填 |

---

## 1. 产品概述

### 1.1 定位
测试工坊 Pro 是**AI 驱动的自动化测试用例生成与执行平台**。测试人员输入 API 定义和业务 PRD，系统自动生成分层测试用例、生成可执行代码、实时执行并产出 ISTQB 标准报告。

### 1.2 核心价值
| 痛点 | 解决方案 |
|------|---------|
| 手工写用例慢、覆盖不全 | AI 分析 PRD 自动生成 + 7 层安检机矩阵 |
| 用例与代码脱节 | 用例预览=执行计划，所见即所测（exact 模式） |
| 执行过程黑盒 | SSE 实时流 + 暂停/继续/停止三态控制 |
| 测试质量不可量化 | 分层覆盖率 + 通过率趋势 + 可上线判断 |
| 业务规则难追溯 | PRD 规则 → 用例 → 执行结果全链路 |

### 1.3 用户角色
| 角色 | 需求 | 核心场景 |
|------|------|---------|
| 测试工程师 | 快速生成+执行用例 | 填 API → 生成 → 预览 → 执行 |
| 测试 Leader | 覆盖评估+质量把关 | 7 层矩阵检查 + 趋势分析 |
| QA 经理 | 可上线判断 | 连续通过 streak≥3 |
| 开发 | 单端点快速自测 | ▶ 快测按钮 |

---

## 2. 功能需求

### FR-1 测试方案配置（Wizard 五步）

#### FR-1.1 模板与项目信息
- **R-101**: 提供 ≥3 个内置模板（电商/百度/Demo），点击模板一键填充全部表单
- **R-102**: Base URL 自动修正：`localhost`→`http://127.0.0.1:8000`、补 `http://` 前缀、去尾部斜杠
- **R-103**: URL 输入实时校验：合法→绿框✅；需修正→蓝框+显示修正后地址；非法→红框⚠
- **R-104**: 认证方式 4 种：无需/自定义Header/Bearer/Basic
- **R-105**: 认证值仅内存存储（不写 localStorage、不写文件），标注"(仅内存存储)"

#### FR-1.2 API 接口配置
- **R-106**: 方法仅 GET/POST 两种
- **R-107**: 每行支持：方法下拉 + 路径输入 + 说明 + ▶ 快测 + 删除
- **R-108**: ▶ 快测：单端点立即执行（无需完整计划）
- **R-109**: 从用例库批量导入（勾选→写入）

#### FR-1.3 Web 页面配置
- **R-110**: 页面路径 + 名称 + 浏览器（Chromium/Firefox/WebKit）
- **R-111**: UI 测试每页 6 项：加载/JS错误/性能/响应式/导航链接/资源加载（+截图共 7 项）

#### FR-1.4 数据规则
- **R-112**: 每行一条规则，生成 Data 测试遍历验证

#### FR-1.5 业务上下文
- **R-113**: PRD 三种导入方式：内置下拉/多文件上传/粘贴
- **R-114**: 📚 全部加载自动触发分析
- **R-115**: AI 分析输出实体/字段/关系/状态机/业务规则
- **R-116**: 分析结果可 JSON 编辑
- **R-117**: 重新分析智能合并（不覆盖更丰富的历史结果）

### FR-2 AI 用例生成

#### FR-2.1 生成策略
- **R-201**: 仅用 AI 生成，失败直接报错（不回退模板）
- **R-202**: 支持推理模型（deepseek-v4-pro）：非流式调用，300s 超时，max_tokens ≥16000
- **R-203**: content 空时兜底读 reasoning_content
- **R-204**: JSON 解析用 JSONDecoder.raw_decode（字符串内花括号不破坏解析）

#### FR-2.2 用例筛查（6 条规则）
- **R-205**: 方法 ∈ {GET,POST}，否则剔除
- **R-206**: 路径以 `/` 或 `http` 开头
- **R-207**: 优先级归一化 P0/P1/P2
- **R-208**: title 必填且 ≤200 字符
- **R-209**: 方法+路径+标题去重
- **R-210**: 路径不匹配配置 → `_path_mismatch` 标记（保留不剔除）

#### FR-2.3 流式体验
- **R-211**: SSE 事件流：info→clear→case×N→done
- **R-212**: 每到达一条用例立即渲染一行
- **R-213**: 四个分区头：🔌API/📱UI/📊Data/🔧Unit，各带条数

### FR-3 7 层安检机测试矩阵

| 层 | 需求编号 | 规则 |
|----|---------|------|
| L1 可达性 | R-301 | 每端点一条 `<500` 检查 |
| L2 字段契约 | R-302 | 实体字段全存在断言 `all(k in d[0])` |
| L3 业务规则 | R-303 | 7 类关键词映射（金额/唯一/权限/状态/必填/上限/通用） |
| L4 状态机 | R-304 | 序列测试 create→verify |
| L5 数据完整性 | R-305 | 跨 API 金额一致性（差<0.02） |
| L6 安全注入 | R-306 | 剥认证头→必须 401/403 |
| L7 边界压力 | R-307 | ThreadPoolExecutor 10 并发全 `<500` |

### FR-4 代码生成

- **R-401**: 生成 5 文件：conftest + test_api + test_ui + test_data + test_unit
- **R-402**: 路径参数 `{xxx}`→`1`
- **R-403**: authValue 经环境变量传递（TW_AUTH_HEADER），不落盘
- **R-404**: conftest 自动登录 fixture（TW_LOGIN_USER/PASS，默认 admin/Admin@123）
- **R-405**: 15 条关键词规则生成针对性断言（见 FR-4 附录表）
- **R-406**: GET/HEAD/OPTIONS/DELETE 不带 json body

### FR-5 执行引擎

- **R-501**: 三态控制：⏸暂停/▶继续/⏹停止
- **R-502**: 暂停=断开 SSE 不杀进程；继续=重连持久队列续读（输出不丢）
- **R-503**: 停止=唯一杀进程入口，清理全部状态
- **R-504**: 10 分钟超时上限
- **R-505**: SSE 实时输出 + 百分比进度
- **R-506**: 完成时自动：存历史 + 存用例库 + 更新用例状态

### FR-6 报告

- **R-601**: JUnit XML 解析，passed = total - failed - errors - skipped
- **R-602**: 通过率趋势（最近10次）+ 连续通过 streak
- **R-603**: 可上线判断：streak≥3 显示 ✅
- **R-604**: 合规导出 JSON（执行历史+用例库）

### FR-7 用例管理（/tc）

- **R-701**: CRUD + 模块分组 + 筛选 + 搜索
- **R-702**: 锁定机制（重生成保留）
- **R-703**: 执行选中

### FR-8 CI/CD（/ci）

- **R-801**: 三阶段流水线：AI生成→路径筛选→执行
- **R-802**: 定时执行 + 一键触发
- **R-803**: 实时日志 + 进度条

### FR-9 安全

- **R-901**: SSRF 防护（进程内 DNS，拒私有/保留/云元数据）
- **R-902**: AI Key AES 加密存储（PBKDF2+HMAC 标签）
- **R-903**: 全量 XSS 转义 he()
- **R-904**: 速率限制（gnr 20/min，plan 30/min）
- **R-905**: 原子写入

---

## 3. 状态机

### 3.1 执行引擎状态机
```
[新建] --POST /api/plan--> PLANS[pid]
[新建] --GET /api/stream--> gen_code --> [运行中]
[运行中] --客户端断开--> [已暂停]（进程存活，输出进持久队列）
[已暂停] --GET /api/stream--> [运行中]（续读队列，计数不重复）
[运行中/已暂停] --POST /api/stop--> [已停止]（杀进程+清状态）
[运行中/已暂停] --pytest自然结束(__END__)--> [完成]（存历史+用例+状态）
[运行中] --10min超时--> [已停止]
```

### 3.2 订单状态机（靶机 mock_ecommerce）
```
created → confirmed → paid → shipped → delivered
created → cancelled
confirmed → cancelled
（delivered 为终态；paid/shipped 取消走退款流程）
```

### 3.3 退款状态机
```
[申请] pending --管理员通过--> approved（恢复库存，订单取消）
              --管理员驳回--> rejected（订单不变）
```

---

## 4. 边界与异常场景

### 4.1 AI 生成
| 场景 | 预期 |
|------|------|
| AI Key 未配置 | 报错"未配置AI Key" |
| AI Key 格式错误（乱码/短） | 报错，不发起 HTTP 调用 |
| AI 返回 401 | 报错"AI API 401" |
| AI 返回空 content | 报错"AI 未产出有效用例(返回0字符)" |
| AI 返回非 JSON | raw_decode 跳过，全跳过则报错 |
| AI 用例全部违规 | 报错"AI 用例全部未通过筛查" |
| AI 生成 100 条 | 全部渲染，序号连续 |

### 4.2 执行
| 场景 | 预期 |
|------|------|
| 被测服务未启动 | 全部 502/拒连，报告如实显示 |
| 暂停后不重连 | 进程跑完自然结束，输出滞留队列 |
| 暂停后重连 | 断线期间输出补发，计数不重复 |
| 停止后再执行 | 全新会话，状态干净 |
| 认证被测系统 | 自动登录 fixture 注入 token |

### 4.3 数据
| 场景 | 预期 |
|------|------|
| 用例库 1000 条 | 截断保留 500 |
| 历史 50 条 | 最旧删除 |
| 生成目录 20 个 | 最旧删除 |
| 计划 >50KB | 拒绝 |

---

## 5. 非功能需求

| 编号 | 需求 |
|------|------|
| N-01 | 模板生成用例秒出（AI 失败不阻塞） |
| N-02 | AI 生成 300s 内完成 |
| N-03 | 测试执行 10min 上限 |
| N-04 | 暂停/继续无数据丢失 |
| N-05 | 支持 Windows/Linux（python 3.8+） |
| N-06 | 无框架前端（原生 JS），零构建 |
| N-07 | 报告 ISTQB/JUnit 标准 |

---

## 6. 验收标准

| 编号 | 标准 |
|------|------|
| A-01 | 电商靶机全流程：登录→商品→订单→退款→统计，138+ 测试通过率 ≥85% |
| A-02 | 百度靶机：138 通过（外部站点真实差异不算缺陷） |
| A-03 | 认证系统：自动登录后受保护接口不再 401 |
| A-04 | 暂停→继续：输出连续无重复计数 |
| A-05 | 报告通过数 = 仪表盘通过数（skipped 扣除一致） |
| A-06 | AI 返回非法方法用例 → 被剔除并提示数量 |
| A-07 | XSS 注入项目名 → 页面不执行脚本 |

---

## 7. 附录：15 条关键词规则（FR-4 附录）

| # | 关键词 | 断言 |
|---|--------|------|
| 1 | sql/sqli/注入/injection | <500 |
| 2 | xss/脚本/script/cross | ≥400 或 "<script>" not in text |
| 3 | 缺少/必填/缺失/空/empty | 400/422/401 |
| 4 | 未认证/未授权/无权限/unauth/token/forbidden | 401/403 |
| 5 | 不存在/404/not found/找不到 | 404/400 |
| 6 | 无效/非法/invalid/格式/bad | 400/422 |
| 7 | 重复/dup/冲突/already | 400/409 |
| 8 | 过短/short/超长/long/过长/溢出 | 400/422 或 <500 |
| 9 | 创建/create/新增/add/注册 | 200/201 |
| 10 | 更新/update/修改/edit/replace | 200/201/204 |
| 11 | 删除/delete/remove | 200/204 |
| 12 | 详情/detail/单个/id/查看/获取 | 200/404 |
| 13 | 分页/page/limit/列表/list/查询/query | 200 |
| 14 | 登录/login/auth | 200/201（env 凭证） |
| 15 | 健康/health/状态/status/ping | 200 |
